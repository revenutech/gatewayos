# =============================================================================
# Revenu Platform — KrakenD ServiceAccount
# =============================================================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: krakend
  labels:
    app.kubernetes.io/name: krakend
    app.kubernetes.io/component: api-gateway
    app.kubernetes.io/part-of: revenu-platform
automountServiceAccountToken: false
