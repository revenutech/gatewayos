"modifier/lua-proxy": {
  "sources": ["/etc/krakend/partials/lua/token_revocation.lua"],
  "pre": "pre_proxy",
  "live": false,
  "allow_open_libs": true,
  "extra_config": {
    "dynamic_config": {
      "redis_host": "{{ .redis.host }}",
      "redis_port": "{{ .redis.port }}"
    }
  }
}